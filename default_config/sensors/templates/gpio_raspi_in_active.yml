calculate: avg_min_max
description: gpio_raspi_in_active
disabled: true
format: int
retain: history
service:
  configuration:
    pin: 2
  mode: active
  name: gpio_raspi_in_active
  schedule:
    minutes: 10
    trigger: interval
